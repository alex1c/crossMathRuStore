import { PlaceholderScreen } from '@/src/screens'

/** Continue active game — placeholder until save/restore lands. */
export default function ContinueRoute() {
	return (
		<PlaceholderScreen
			title="Продолжить"
			description="Возобновление сохранённой партии появится позже."
		/>
	)
}
