/**
 * App services layer.
 * Persistence, notifications, ads, and analytics adapters land in later phases.
 * Keep this module free of secrets and unused SDKs in Phase 0.
 */

export const SERVICES_PHASE = 0 as const
